// Configure the AWS provider 
// The provider block specifies the AWS region where your resources will be created
// In this example, we are using the "us-west-1" region, but you can change it to any region you prefer
provider "aws" {
    region = "us-west-1"  
}

